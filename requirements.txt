# ── Telegram Bot Framework ──
python-telegram-bot[job-queue]>=21.6

# ── Image & QRIS / Invoice Generator ──
qrcode[pil]>=8.0
Pillow>=10.0.0

# ── HTTP Client & Environment Config ──
httpx>=0.27.0
python-dotenv>=1.0.1

# ── Webhook Server (FastAPI) ──
fastapi>=0.115.0
uvicorn[standard]>=0.30.6
pydantic>=2.0.0
anyio>=4.0.0
